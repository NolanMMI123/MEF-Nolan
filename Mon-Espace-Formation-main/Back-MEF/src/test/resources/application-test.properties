spring.application.name=backend-test

# Important: durant les tests, on n'initialise pas de données de démo via DataInitializer (profil géré via @Profile("!test")).
# On laisse la config Mongo par défaut hors test, et on évite toute connexion DB tant que rien ne l'utilise.
